<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".activities.MainActivity">

    <ListView
        android:layout_margin="5sp"
        android:id="@+id/shopping_item_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/add_shopping_list_btn"
        android:layout_alignParentTop="true" />

    <TextView
        android:layout_margin="10sp"
        android:id="@+id/no_shopping_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="23sp"
        android:textStyle="italic"
        android:gravity="center"
        android:layout_centerInParent="true"
        android:text="@string/no_shopping_list"/>

    <Button
        android:layout_margin="10sp"
        android:id="@+id/add_shopping_list_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:onClick="showCreateShoppingListDialog"
        android:text="@string/new_shopping_list" />


</RelativeLayout>